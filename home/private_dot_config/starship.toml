# Get editor completions based on the config schema
"$schema" = 'https://starship.rs/config-schema.json'

# Inserts a blank line between shell prompts
add_newline = true

format = """
$directory$git_branch$git_state $git_status$fill$cmd_duration
$character\
"""

[fill]
symbol = ' '

[directory]
style = 'bold blue'

[git_branch]
style = 'green'
symbol = ' '
format = '[$symbol$branch]($style)'

[git_status]
ignore_submodules = true
untracked = '?$count'
staged = '+$count'
stashed = '\$$count'
modified = '!$count'
renamed = '»$count'
deleted = '✘$count'
style = 'bright-blue'
format = '(([$all_status]($style) )[$ahead_behind](green) )'

[nodejs]
disabled = true
